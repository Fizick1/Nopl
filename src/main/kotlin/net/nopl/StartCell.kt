package net.nopl

/**
 * Сoordinates of the initial cell
 * @param startColumn coordinate of the initial column
 * @param startRow coordinate of the initial line
 */
class StartCell(val startColumn: Int = 0, val startRow: Int = 0)